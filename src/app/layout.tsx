export const metadata = {
  title: "Agent Economy — Admin Dashboard",
  description: "Platform admin for the Agent Economy",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
